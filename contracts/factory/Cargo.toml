[package]
name = "factory"
version = "0.1.0"
edition = "2021"
description = "ChainChit - Factory for ChitGroup contracts"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
