[package]
name = "dispute"
version = "0.1.0"
edition = "2021"
description = "ChainChit - On-chain dispute resolution with multi-sig arbitration"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
