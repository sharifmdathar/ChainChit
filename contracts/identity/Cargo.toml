[package]
name = "identity"
version = "0.1.0"
edition = "2021"
description = "ChainChit - Social attestation identity layer"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
