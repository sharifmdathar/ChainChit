[package]
name = "reputation"
version = "0.1.0"
edition = "2021"
description = "ChainChit - On-chain reputation tracking for chit fund members"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
