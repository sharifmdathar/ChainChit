[package]
name = "chit_group"
version = "0.1.0"
edition = "2021"
description = "ChainChit - On-chain chit fund group lifecycle management"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
